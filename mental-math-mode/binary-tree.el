;; Binary tree.
(defun make-binary-tree ()
  "Make binary tree."
  (let ((result (cons (make-node) (make-node))))
    result))

(defun left-tree (tree)
  "Get left subtree."
  ())

(defun right-tree (tree)
  "Get left subtree."
  ())
	 
(provide 'binary-tree)
