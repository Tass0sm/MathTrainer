;; This will start as a simple binary addition but grow to involve
;; multiple operators and different operations.
(defun make-problem (op-one op-two)
  "Create a problem with op-one and op-two"
  (let ()
    ()
    result))


(provide 'problem)
